---
phase: 01-auth-and-release-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/app.ts, backend/src/middleware/rateLimit.ts, backend/src/middleware/error.ts, backend/src/routes/auth.ts, backend/src/routes/users.ts, backend/src/services/supabaseAuthService.ts, backend/src/services/userService.ts, mobile/src/context/AuthContext.tsx, mobile/src/api/client.ts, admin/src/api/client.ts, mobile/tests/app-store-readiness.test.mjs, backend/tests/*]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
user_setup: []
must_haves:
  truths:
    - Mobile and web auth flows stay on the correct surface and do not redirect users into the wrong product experience.
    - Backend auth/config paths fail closed where practical and do not quietly fall back to insecure defaults.
    - Abuse-prone auth routes are throttled, and sensitive admin payloads expose only the fields required by the consuming surface.
  artifacts:
    - backend/src/app.ts
    - backend/src/middleware/rateLimit.ts
    - backend/src/middleware/error.ts
    - backend/src/routes/auth.ts
    - backend/src/routes/users.ts
    - backend/src/services/supabaseAuthService.ts
    - backend/src/services/userService.ts
    - mobile/src/context/AuthContext.tsx
    - mobile/src/api/client.ts
    - admin/src/api/client.ts
    - mobile/tests/app-store-readiness.test.mjs
    - backend/tests
  key_links:
    - Auth hardening must preserve the current intended OTP, password, biometric, and session restore behavior while closing obvious leakage and abuse gaps.
    - Security changes should improve trust without adding unnecessary friction for normal daily gym use.
---

<objective>
Harden shared auth and session flows across backend, mobile, and admin web so that login is reliable, low-leakage, and less abuse-prone.

Purpose: The current product depends on auth working every day for real users. This plan closes the biggest security and reliability gaps in those flows without changing the product model.
Output: Hardened auth/config/session code, narrower sensitive payload exposure, and regression tests that prove the intended behavior.
</objective>

<execution_context>
@/Users/forresttimm/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-and-release-reliability/01-CONTEXT.md
@backend/src/app.ts
@backend/src/middleware/auth.ts
@backend/src/middleware/role.ts
@backend/src/middleware/rateLimit.ts
@backend/src/middleware/error.ts
@backend/src/routes/auth.ts
@backend/src/routes/users.ts
@backend/src/services/authService.ts
@backend/src/services/supabaseAuthService.ts
@backend/src/services/userService.ts
@backend/src/utils/config.ts
@mobile/src/context/AuthContext.tsx
@mobile/src/api/client.ts
@admin/src/api/client.ts
@mobile/tests/app-store-readiness.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for auth hardening expectations</name>
  <files>mobile/tests/app-store-readiness.test.mjs, backend/tests/*</files>
  <action>Add focused failing tests that lock in the intended security and reliability behavior before implementation. Cover at least: no cross-surface redirect in mobile OTP send, fail-closed CORS/config expectations, rate limiting on abuse-prone auth routes, and no raw backend error leakage on client-facing auth failures.</action>
  <verify>cd mobile && node --test tests/app-store-readiness.test.mjs</verify>
  <done>New or updated tests capture the intended auth/reliability behavior and fail until the implementation is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Harden backend auth, config, and sensitive response surfaces</name>
  <files>backend/src/app.ts, backend/src/middleware/rateLimit.ts, backend/src/middleware/error.ts, backend/src/routes/auth.ts, backend/src/routes/users.ts, backend/src/services/supabaseAuthService.ts, backend/src/services/userService.ts, backend/src/utils/config.ts</files>
  <action>Implement the backend security hardening required by the tests. Prefer fail-closed config behavior over wildcard defaults, add rate limiting where auth/invite abuse is possible, stop returning overly detailed internal error messages to clients, and narrow admin payloads where fields like QR codes or login timestamps are not required by the consuming flow.</action>
  <verify>cd backend && npm run build</verify>
  <done>Backend auth and invite flows are more resistant to abuse and misconfiguration, and sensitive data exposure is reduced without breaking intended admin workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve correct mobile and admin session behavior after hardening</name>
  <files>mobile/src/context/AuthContext.tsx, mobile/src/api/client.ts, admin/src/api/client.ts, mobile/tests/app-store-readiness.test.mjs</files>
  <action>Adjust mobile and admin auth/session handling as needed so the hardening work does not regress normal login, session restore, biometric login, or bootstrap behavior. Keep the code-paths explicit and test-backed.</action>
  <verify>cd mobile && npx tsc --noEmit && node --test tests/app-store-readiness.test.mjs</verify>
  <done>Normal login and session flows still work after the hardening changes, and the shared auth regressions remain covered by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && npm run build`
- [ ] `cd mobile && npx tsc --noEmit`
- [ ] `cd mobile && node --test tests/app-store-readiness.test.mjs`
- [ ] Backend auth routes do not fail open on missing config
- [ ] Mobile and admin login flows remain on the correct product surface
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app is more secure by default without breaking current intended onboarding and session behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-release-reliability/01-01-SUMMARY.md`
</output>
