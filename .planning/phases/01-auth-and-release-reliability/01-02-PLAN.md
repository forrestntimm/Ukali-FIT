---
phase: 01-auth-and-release-reliability
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified: [mobile/package.json, mobile/scripts/build-local-ios.sh, mobile/scripts/validate-ios-ipa.sh, mobile/APP_STORE_PRECHECKLIST.md, mobile/APP_STORE_RELEASE.md, mobile/tests/app-store-readiness.test.mjs]
autonomous: true
requirements: [RELS-01, RELS-02]
user_setup: []
must_haves:
  truths:
    - Athlete and coach/admin local iOS builds follow one repeatable repo-visible process.
    - Release validation proves embedded bundle and required asset presence instead of assuming them.
    - Build numbering and export rules are predictable enough to avoid repeated Transporter/TestFlight churn.
  artifacts:
    - mobile/package.json
    - mobile/scripts/build-local-ios.sh
    - mobile/scripts/validate-ios-ipa.sh
    - mobile/APP_STORE_PRECHECKLIST.md
    - mobile/APP_STORE_RELEASE.md
    - mobile/tests/app-store-readiness.test.mjs
  key_links:
    - Local-only release remains a non-negotiable project constraint.
    - Release documentation and validation scripts must describe the same source of truth, not parallel inconsistent flows.
---

<objective>
Standardize and verify the local release workflow for both mobile variants so shipping stops depending on session memory and manual guesswork.

Purpose: Release reliability is part of product reliability in this repo. The build/export/sign/upload path should be explicit, validated, and hard to misuse.
Output: Tightened local build scripts, aligned release docs, and release-readiness checks that protect against bad artifacts.
</objective>

<execution_context>
@/Users/forresttimm/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-and-release-reliability/01-CONTEXT.md
@mobile/package.json
@mobile/scripts/build-local-ios.sh
@mobile/scripts/validate-ios-ipa.sh
@mobile/APP_STORE_PRECHECKLIST.md
@mobile/APP_STORE_RELEASE.md
@mobile/tests/app-store-readiness.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add release-readiness regression coverage for the local path</name>
  <files>mobile/tests/app-store-readiness.test.mjs</files>
  <action>Add or tighten release-readiness tests so they explicitly check the local-only assumptions this repo depends on: no cloud EAS usage, variant-aware build expectations, JS bundle embedding checks, and any current path-based IPA validation invariants.</action>
  <verify>cd mobile && node --test tests/app-store-readiness.test.mjs</verify>
  <done>The release-readiness test suite makes the local-only and IPA-validity assumptions explicit and enforceable.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate build and IPA validation behavior</name>
  <files>mobile/package.json, mobile/scripts/build-local-ios.sh, mobile/scripts/validate-ios-ipa.sh</files>
  <action>Tighten the local iOS build and validation scripts so versioning, export, and artifact checks follow one predictable path for athlete and coach/admin. Prefer improving the current scripts over creating parallel new ones.</action>
  <verify>cd mobile && ./scripts/validate-ios-ipa.sh \"$(ls -t local-builds/*.ipa 2>/dev/null | head -n 1)\"</verify>
  <done>Build and validation scripts agree on what a valid release artifact is, and that logic is visible in the repo.</done>
</task>

<task type="auto">
  <name>Task 3: Align release docs to the actual local workflow</name>
  <files>mobile/APP_STORE_PRECHECKLIST.md, mobile/APP_STORE_RELEASE.md, mobile/package.json</files>
  <action>Update the release docs so they describe the real local-only process, including artifact validation, Transporter upload expectations, and build-number discipline. Remove ambiguity that could cause duplicate build-number or bad-artifact submissions.</action>
  <verify>cd mobile && rg -n \"local|Transporter|build number|validate-ios-ipa|cloud\" APP_STORE_PRECHECKLIST.md APP_STORE_RELEASE.md package.json</verify>
  <done>The written release flow matches the scripts and current project constraints instead of relying on tribal knowledge.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd mobile && node --test tests/app-store-readiness.test.mjs`
- [ ] Local release docs and scripts describe the same workflow
- [ ] Variant builds remain local-only and validation-backed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Athlete and coach/admin release work is reproducible from repo-visible instructions and scripts
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-release-reliability/01-02-SUMMARY.md`
</output>
