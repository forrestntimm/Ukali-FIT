---
phase: 01-auth-and-release-reliability
plan: 03
type: execute
wave: 1
depends_on: [01, 02]
files_modified: [mobile/App.tsx, mobile/src/context/AuthContext.tsx, mobile/src/lib/appCrashHandler.ts, mobile/src/lib/sessionGuard.ts, mobile/src/components/AppErrorBoundary.tsx, mobile/src/components/AppLoadingScreen.tsx, mobile/tests/app-store-readiness.test.mjs]
autonomous: true
requirements: [AUTH-02, RELS-03]
user_setup: []
must_haves:
  truths:
    - Startup paths fail gracefully whenever possible instead of turning normal recoverable problems into hard crashes.
    - Session restore and cached fallback behavior are explicit and bounded so the wrong user state is not silently shown.
    - The app shows a meaningful startup/loading shell quickly while backend/session work continues in the background.
  artifacts:
    - mobile/App.tsx
    - mobile/src/context/AuthContext.tsx
    - mobile/src/lib/appCrashHandler.ts
    - mobile/src/lib/sessionGuard.ts
    - mobile/src/components/AppErrorBoundary.tsx
    - mobile/src/components/AppLoadingScreen.tsx
    - mobile/tests/app-store-readiness.test.mjs
  key_links:
    - Startup hardening must not break the saved-login, biometric-login, or session-persistence behavior already intended for the app.
    - Recovery behavior should reduce user-facing crashes while still surfacing invalid sessions and real auth failures clearly.
---

<objective>
Reduce startup crash risk and improve startup recovery behavior so the app opens reliably even when auth or network conditions are imperfect.

Purpose: Startup is one of the most fragile trust points in this product. This plan makes cold-open and relaunch behavior more resilient without hiding real account problems.
Output: Hardened startup/error-boundary/session-restore code plus test coverage for the intended recovery behavior.
</objective>

<execution_context>
@/Users/forresttimm/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-and-release-reliability/01-CONTEXT.md
@mobile/App.tsx
@mobile/src/context/AuthContext.tsx
@mobile/src/lib/appCrashHandler.ts
@mobile/src/lib/sessionGuard.ts
@mobile/src/components/AppErrorBoundary.tsx
@mobile/src/components/AppLoadingScreen.tsx
@mobile/tests/app-store-readiness.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for startup-recovery invariants</name>
  <files>mobile/tests/app-store-readiness.test.mjs</files>
  <action>Add or tighten tests for startup-recovery expectations: guarded session reads, explicit fallback behavior, and startup shell/error-boundary presence where the app depends on them to avoid hard crashes.</action>
  <verify>cd mobile && node --test tests/app-store-readiness.test.mjs</verify>
  <done>The intended startup safety behavior is encoded in tests before further implementation changes.</done>
</task>

<task type="auto">
  <name>Task 2: Harden startup/bootstrap error handling</name>
  <files>mobile/App.tsx, mobile/src/context/AuthContext.tsx, mobile/src/lib/appCrashHandler.ts, mobile/src/lib/sessionGuard.ts, mobile/src/components/AppErrorBoundary.tsx, mobile/src/components/AppLoadingScreen.tsx</files>
  <action>Improve startup guardrails so recoverable auth/session/network issues result in safe fallback UI or bounded retry behavior instead of hard app termination. Preserve the current branded loading experience and intended sign-in persistence.</action>
  <verify>cd mobile && npx tsc --noEmit</verify>
  <done>The app remains responsive during startup trouble, and hard crashes are reduced where a controlled fallback is possible.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify release-readiness after startup hardening</name>
  <files>mobile/App.tsx, mobile/src/context/AuthContext.tsx, mobile/tests/app-store-readiness.test.mjs</files>
  <action>Run the full mobile readiness checks after the startup changes and make any small follow-up fixes required to keep release-readiness, saved login, and startup shell behavior aligned.</action>
  <verify>cd mobile && node --test tests/app-store-readiness.test.mjs && npx tsc --noEmit</verify>
  <done>Startup hardening and release-readiness stay aligned, with no regression in the intended mobile auth experience.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd mobile && node --test tests/app-store-readiness.test.mjs`
- [ ] `cd mobile && npx tsc --noEmit`
- [ ] Startup paths prefer controlled recovery over avoidable hard crashes
- [ ] Cached session fallback never crosses account boundaries
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The mobile app is more resilient at startup while preserving intended login persistence and release safety
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-release-reliability/01-03-SUMMARY.md`
</output>
