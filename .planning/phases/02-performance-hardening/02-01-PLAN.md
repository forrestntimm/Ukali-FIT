---
phase: 02-performance-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/routes/classes.ts, backend/src/routes/scheduling.ts, backend/src/routes/users.ts, backend/src/services/classService.ts, backend/src/services/userService.ts, backend/tests/*, admin/tests/performance-cache.test.mjs, mobile/tests/app-store-readiness.test.mjs]
autonomous: true
requirements: [CLAS-01, CLAS-02, CLAS-03, CLAS-04]
user_setup: []
must_haves:
  truths:
    - Classes and scheduling routes return only the data the current screen actually needs.
    - Payload trimming does not break class assignment, counts, or membership-dependent behavior.
    - Performance improvements are visible in repo-backed tests and not just inferred from manual perception.
  artifacts:
    - backend/src/routes/classes.ts
    - backend/src/routes/scheduling.ts
    - backend/src/routes/users.ts
    - backend/src/services/classService.ts
    - backend/src/services/userService.ts
    - backend/tests
    - admin/tests/performance-cache.test.mjs
    - mobile/tests/app-store-readiness.test.mjs
  key_links:
    - Backend payload size is the first place to cut visible load time for mobile classes and admin scheduling.
    - Query/lightweight route changes must preserve the correctness work already done in Phase 1 and upcoming Phase 3.
---

<objective>
Profile and trim backend payload hotspots that are still causing slow classes and scheduling experiences across mobile and admin web.

Purpose: The clients can only feel fast if the server stops handing them oversized responses for simple views.
Output: Leaner classes, scheduling, and user-targeting payloads with regression checks that protect the lighter contract.
</objective>

<execution_context>
@/Users/forresttimm/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-performance-hardening/02-CONTEXT.md
@backend/src/routes/classes.ts
@backend/src/routes/scheduling.ts
@backend/src/routes/users.ts
@backend/src/services/classService.ts
@backend/src/services/userService.ts
@admin/tests/performance-cache.test.mjs
@mobile/tests/app-store-readiness.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for lightweight classes and scheduling payload expectations</name>
  <files>backend/tests/*, admin/tests/performance-cache.test.mjs, mobile/tests/app-store-readiness.test.mjs</files>
  <action>Add or tighten tests around summary payloads, bounded list sizes, and route-specific field expectations for the classes and scheduling surfaces that are still too slow.</action>
  <verify>cd backend && npm run build</verify>
  <done>Payload-light expectations are explicit in tests before query and route changes are made.</done>
</task>

<task type="auto">
  <name>Task 2: Trim backend payload hotspots without breaking correctness</name>
  <files>backend/src/routes/classes.ts, backend/src/routes/scheduling.ts, backend/src/routes/users.ts, backend/src/services/classService.ts, backend/src/services/userService.ts</files>
  <action>Reduce oversized route responses and query cost for classes, scheduling, and admin-targeting flows. Keep required counts and assignment data intact while removing fields the active surfaces do not render.</action>
  <verify>cd backend && npm run build</verify>
  <done>Backend responses for the slowest views are visibly lighter and still correct.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify mobile and admin consumers against the lighter contract</name>
  <files>admin/tests/performance-cache.test.mjs, mobile/tests/app-store-readiness.test.mjs</files>
  <action>Re-run and tighten consumer-side tests so both admin web and mobile stay aligned with the lighter backend contract.</action>
  <verify>cd admin && npm run build && cd ../mobile && node --test tests/app-store-readiness.test.mjs</verify>
  <done>Client expectations are updated and protected against drifting back to heavier payload assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && npm run build`
- [ ] `cd admin && npm run build`
- [ ] `cd mobile && node --test tests/app-store-readiness.test.mjs`
- [ ] Classes/scheduling payloads are explicitly lighter than the previous general-purpose forms
- [ ] No correctness regression is introduced for counts or assignments
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The slowest classes and scheduling views are no longer paying for oversized backend responses
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-hardening/02-01-SUMMARY.md`
</output>
