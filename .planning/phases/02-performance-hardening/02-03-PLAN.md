---
phase: 02-performance-hardening
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified: [admin/src/App.tsx, admin/src/api/client.ts, admin/src/lib/pageCache.ts, admin/src/pages/DashboardPage.tsx, admin/src/pages/MembersPage.tsx, admin/src/pages/PaymentsPage.tsx, admin/src/pages/SchedulingPage.tsx, admin/tests/performance-cache.test.mjs]
autonomous: true
requirements: [CLAS-03, CLAS-04]
user_setup: []
must_haves:
  truths:
    - Admin web pages render meaningful content quickly instead of waiting on heavy bootstrap and full-page fetches.
    - Scheduling and adjacent admin pages reuse lighter caches and avoid expensive eager rendering where possible.
    - Performance changes stay compatible with current admin workflows and data correctness.
  artifacts:
    - admin/src/App.tsx
    - admin/src/api/client.ts
    - admin/src/lib/pageCache.ts
    - admin/src/pages/DashboardPage.tsx
    - admin/src/pages/MembersPage.tsx
    - admin/src/pages/PaymentsPage.tsx
    - admin/src/pages/SchedulingPage.tsx
    - admin/tests/performance-cache.test.mjs
  key_links:
    - The user has repeatedly called out Scheduling as intolerably slow, so this plan must treat it as a first-class target.
    - Admin web changes should build on the lighter backend payloads from 02-01 rather than papering over slow routes in the browser alone.
---

<objective>
Reduce admin web page-load and switch-cost for scheduling and adjacent operational pages so staff can move through the site without long blocking waits.

Purpose: The web admin must feel immediate enough for real front-desk and coaching use, especially around scheduling and payments.
Output: Tighter admin page caching/rendering behavior plus tests that lock in the intended fast path.
</objective>

<execution_context>
@/Users/forresttimm/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-performance-hardening/02-CONTEXT.md
@admin/src/App.tsx
@admin/src/api/client.ts
@admin/src/lib/pageCache.ts
@admin/src/pages/DashboardPage.tsx
@admin/src/pages/MembersPage.tsx
@admin/src/pages/PaymentsPage.tsx
@admin/src/pages/SchedulingPage.tsx
@admin/tests/performance-cache.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red admin-web performance tests for scheduling and shared cache paths</name>
  <files>admin/tests/performance-cache.test.mjs</files>
  <action>Add or tighten tests for admin page-cache reuse, scheduling fast-path behavior, and avoidance of expensive eager rendering or oversized bootstrap dependencies.</action>
  <verify>cd admin && node --test tests/performance-cache.test.mjs</verify>
  <done>Admin performance intent is explicit and test-backed before further implementation changes.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce admin web bootstrap and scheduling view cost</name>
  <files>admin/src/App.tsx, admin/src/api/client.ts, admin/src/lib/pageCache.ts, admin/src/pages/DashboardPage.tsx, admin/src/pages/MembersPage.tsx, admin/src/pages/PaymentsPage.tsx, admin/src/pages/SchedulingPage.tsx</files>
  <action>Refine shared cache use, page bootstrap, and view rendering so the admin web surface, especially Scheduling, paints quickly and reuses warm data wherever appropriate.</action>
  <verify>cd admin && npm run build</verify>
  <done>Admin pages avoid repeated heavy bootstrap work and Scheduling feels materially lighter.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify the deployed-ready admin performance path</name>
  <files>admin/tests/performance-cache.test.mjs</files>
  <action>Run the admin performance test suite and make any follow-up fixes needed so the production-facing fast path is locked in before deployment work starts.</action>
  <verify>cd admin && node --test tests/performance-cache.test.mjs && npm run build</verify>
  <done>Admin web performance improvements are verified and ready for the usual deploy path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd admin && node --test tests/performance-cache.test.mjs`
- [ ] `cd admin && npm run build`
- [ ] Scheduling and related admin pages reuse warm cache and lighter routes where possible
- [ ] No admin workflow correctness regressions are introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Admin web, especially Scheduling, no longer pays avoidable page-load and render penalties
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-hardening/02-03-SUMMARY.md`
</output>
