# Plan 03-03: Verify Scheduling And Class Assignment Correctness

## Goal

Make scheduling and class assignment behavior line up across admin web, backend, and coach mobile.

## Scope

- Verify primary/secondary coach assignment semantics across scheduling routes and class list routes.
- Ensure coach mobile schedule surfaces include the right assigned classes.
- Confirm assignment changes propagate to scheduling, coach dashboards, and check-in flows.
- Tighten tests for duplicate coach assignment prevention and scheduling payload shape.

## Acceptance Criteria

- Primary and secondary coach assignments are represented consistently in scheduling payloads.
- Coach mobile sees classes assigned to that coach in either role.
- Scheduling rejects duplicate primary/secondary assignments for the same class.
- Existing performance payload protections remain intact.

## Verification

- `cd backend && node --test tests/payment-plans.test.mjs tests/performance-payloads.test.mjs`
- `cd admin && node --test tests/performance-cache.test.mjs`
- `cd mobile && node --test tests/app-store-readiness.test.mjs`
